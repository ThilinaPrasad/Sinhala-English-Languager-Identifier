*) This project included two sections,
	1. Trainer : Train the model and save the model weights into "weights.hdf5" file.
	2. Predictor: Load the built model using "weights.hdf5" weights and do the prediction.

*) arr.npy and data.csv are the intermediate files that generated while training the model.